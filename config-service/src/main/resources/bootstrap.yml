server:
  port:8083

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/digitallab-academy/ms-course-youtube.git
          searchPath: config-data
          username: ${GIT_USER}
          password: ${GIT-PASSWORD}
  security:
    user:
      name: root
      password: s3cr3t

